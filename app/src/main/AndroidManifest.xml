<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="il.non.celiacc">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:layoutDirection="rtl"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:textDirection="rtl"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:label="אפליקציית עמותת הצליאק בישראל">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenu"
            android:label="תפריט ראשי" />
        <activity
            android:name=".Users.UpdateUser"
            android:label="עדכון פרטי משתמש" />
        <activity
            android:name=".Users.NewUserForm"
            android:label="רישום משתמש חדש" />
        <!--<activity-->
            <!--android:name=".Search"-->
            <!--android:label="חיפוש ידני"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->
        <activity
            android:name=".BarcodeScan"
            android:label="סריקת ברקוד" />
        <activity
            android:name=".Products.SearchProductActivity"
            android:label="חיפוש מוצר" />
        <!--<activity-->
            <!--android:name=".ProductSearchResult"-->
            <!--android:label="תוצאות החיפוש" />-->

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Categories.CategoryGridActivity"
            android:label="חיפוש לפי קטגוריות" />
        <activity
            android:name=".SubCategories.SubCatListItemResult"
            android:label="תוצאות תת הקטגוריה" />
        <activity android:name=".SubCategories.SubCategoryGridActivity" />
        <activity android:name=".RetrievePass"></activity>
    </application>

</manifest>